# GitHub “About” box — paste these

Edit on the repo: **⚙ Settings** (or **About** → ✎) next to the repo description.

## Short description (paste into the description field)

**Limit: 350 characters** (GitHub hard cap). This one is **~280 chars**:

> Local-only HTML: your Spellbook + GitHub stars as **interactive graphs**—not another bookmark list. Built for brains that think in **remixes** (Ableton energy): drag, cluster, read READMEs in context, and chase **“what if I combine this with *that*?”** serendipity instead of scrolling spreadsheets.

**Shorter (~160 chars)** if you want more headroom:

> Spellbook + stars as living graphs—**remix** your capabilities like samples in a DAW. Non-linear, ADHD-friendly: **combine this with that**, serendipity over lists. Static HTML; run locally.

## Topics (add under the description)

Suggested tags (pick what fits):

`visualization` `knowledge-graph` `github-stars` `graph` `static-site` `readme` `serendipity`

## Website field

Leave blank (no hosted app) **or** put your repo URL:  
`https://github.com/nickarchuleta/graphify-viewer`

---

## Screenshot flair (assessment)

What’s in **`docs/screenshots/`** for the README:

| Image | Flair | Notes |
|--------|--------|--------|
| **`stars-nebula.png`** | **Strong** | Hub-and-spoke “supernova” reads instantly: one glance = whole star field. High contrast, reads at thumbnail size on GitHub. |
| **`unified-readme.png`** | **Strong** | Shows the **product story**: two views + README panel—proves it’s not only pretty, it’s **usable**. |

**Tips:** Keep `width="720"` (or similar) so the README doesn’t feel huge on mobile. Captions under images are doing real work—keep them. If you ever re-export, avoid visible `/Users/...` paths in the crop.

The **screenshots of the GitHub repo page itself** (repo UI + README preview) are great for **tweets/slides**, not usually committed—they’re meta, not product flair.
